# Copy to: /etc/kaiser/kaiser.env
# Make sure this file is readable by the kaiser service user.

# All kaiser-specific env vars are prefixed with KAISER_.
# (Exception: PI_CODING_AGENT_DIR is owned by pi-coding-agent.)

# Postgres
KAISER_DATABASE_URL=postgres://user:pass@127.0.0.1:5432/kaiser

# Path to the user config repo (workflows/, .pi/, AGENTS.md, ...)
KAISER_WORKSPACE_DIR=/var/lib/kaiser/workspace

# Server bind
KAISER_PORT=31415

# Optional: isolate pi-coding-agent state (sessions, global settings)
# PI_CODING_AGENT_DIR=/var/lib/kaiser/pi-agent

# Provider credentials (example)
# OPENAI_API_KEY=...

# Telegram (optional)
# KAISER_TELEGRAM_BOT_TOKEN=...
# KAISER_TELEGRAM_INGEST_MODE=polling
